### 네트워크 연결
#### 속성
연결된 네트워크 값들을 확인할 수 있다.  
#### 연결별 DNS 접미사
아무런 값이 할당되지 않는다. DNS는 Domain Name Service로 아이피 주소가 아닌 
#### 설명
현재 사용하고 있는 랜 드라이버 명칭이 나온다.
#### 물리적 주소
mac adress를 의미한다.  
#### DHCP 사용 (Dynamic Host Configuration Protocol)
```
ipconfig /all
```
192.168.0.1는 기본 게이트 웨이, DHCP 서버이며 모든 네트워크 장치의 기본 주소이다.  
네트워크 IP 주소는 무한한 것이 아닌 제한적이다. 

#### IPv4 주소


#### IPv4 서브넷 마스크
#### 임대 시작 날짜
#### 임대 만료 날짜
#### IPv4 기본 게이트웨이
